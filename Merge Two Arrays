import java.util.*;

public class Main {
    public static void main(String[] args) {

        int[] arr1 = {1, 3, 5};
        int[] arr2 = {2, 4, 6};

        int[] merged = new int[arr1.length + arr2.length];

        // copy arr1
        for (int i = 0; i < arr1.length; i++) {
            merged[i] = arr1[i];
        }

        // copy arr2
        for (int i = 0; i < arr2.length; i++) {
            merged[arr1.length + i] = arr2[i];
        }

        // sort
        Arrays.sort(merged);

        System.out.println(Arrays.toString(merged));
    }
}




public class MergeTwoArrays {
    public static void main(String[] args) {
        int [] arr1 = {1,2,3,4,5};
        int [] arr2 = {6,7,8,9,10};
        int n = arr1.length;
        int m = arr2.length;
        int i=0, j=0, k=0;
        int [] result = new int[n+m];

// put the value of arr1 and arr2 in result 
        while(i<n && j<m){
            if(arr1[i]<=arr2[j]){
             // k++ and i++ ka matlab hai yaha pelhe kai mai value store kre and then k++ krdo and i ki value lo or i ko i++ krdo   
              result[k++]=arr1[i++];
            }else{
                result[k++] = arr2[j++];
            }
        }
       // Copy remaining elements from arr1 if anything is left
        while(i<n){
            result[k++] = arr1[i++];
        }
        //Copy remaining elements from arr2 if anything is left
        while(j<m){
            result[k++] = arr2[j++];
        }
        System.out.print("Result is : " );
        for(int x:result){
            System.out.print(x+" " );
        }
}
}
